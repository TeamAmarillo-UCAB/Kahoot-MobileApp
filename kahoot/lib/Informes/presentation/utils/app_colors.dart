import 'package:flutter/material.dart';

class AppColors {
  // --- Primarios ---
  // Amarillo Mostaza (AppBar, Tarjetas Resumen, Fondos destacados)
  static const Color primaryYellow = Color(0xFFFFB300);
  
  // Marrón Ocre (Items de preguntas, botones secundarios, acentos)
  static const Color cardBrown = Color(0xFFAF6301);
  
  // Negro/Marrón Muy Oscuro (Fondo general Scaffold)
  static const Color backgroundDark = Color(0xFF3A240C);
  // --- Estados ---
  static const Color errorRed = Color(0xFFFF0000);
  static const Color successGreen = Color(0xFF00C853);

  // --- Texto y Neutros ---
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textBlack = Color(0xFF000000);
  
  // Color para sombras sutiles
  static const Color shadowBlack = Color.fromRGBO(4, 4, 1, 0.74);
}